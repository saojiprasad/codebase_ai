{
  "name": "video-splitter-for-shorts",
  "version": "1.0.0",
  "description": "Upload one long video and automatically generate edited viral shorts with captions, thumbnails, sound design, and SEO metadata.",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd backend && node server.js",
    "start:frontend": "cd frontend && npx ng serve --port 4200 --open",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npx ng serve --port 4200 --open"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
